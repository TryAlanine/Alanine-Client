/* eslint-disable */

import * as redom from 'redom';

import './scss/index.scss';

export default class Alanine {
  constructor(options) {
    this._version = '0.0.0';
    this.init(options);
  }

  init(options) {
    console.log('PLACEHOLDER');
  }
}

