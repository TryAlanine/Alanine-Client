/* eslint-disable */

import * as redom from 'redom';

import './scss/index.scss';

export default class Alanine {
  constructor(options) {
    this._version = '0.0.0';
  }
}

/*
init:
request meta data ->
construct dom ->
bind event ->
mout dom
*/
